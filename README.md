# git-training

A shared Python project used to practice Git workflows: branching,
pull requests, code review, rebase, and conflict resolution.

## Structure

- `src/greet.py` — greeting functions, one per developer
- `tests/test_greet.py` — unit tests

## Running tests

```bash
python -m pytest tests/